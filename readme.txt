The packages pandas, sklearn, tqdm are needed to run this code locally.
Packages can be installed using:

pip3 install pandas
pip3 install sklearn

How To Run :

python3 12.py

It takes approximately 5mins time to show the output in terminal because of larger C values.